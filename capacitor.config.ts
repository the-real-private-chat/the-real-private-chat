import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'the-real-private-chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
